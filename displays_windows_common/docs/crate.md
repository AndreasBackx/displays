`displays_windows_common` contains shared Windows display helpers used by the `displays` Windows backends.

Most users should start with the top-level `displays` crate instead of depending on this helper crate directly.

See the project root `README.md` for the overall API and crate graph.
